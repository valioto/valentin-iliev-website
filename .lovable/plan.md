Replace the Amazon Marketing Cloud collapsed summary text on the homepage.

## Change
In `src/pages/Index.tsx`, update the `collapsedDescription` for the Amazon Marketing Cloud service card from:

"Go beyond standard reporting. I use SQL-based queries in AMC to connect your first-party data with Amazon's signals, revealing the true path to purchase and attribution across channels."

to:

"Go beyond standard reporting. I use Amazon Marketing Cloud (AMC) to connect your first-party data with Amazon's signals, revealing the path to purchase across channels."

## Scope
- Only this one line in `src/pages/Index.tsx` is changed.
- No changes to the expanded AMC content, other cards, layout, styling, or behaviour.